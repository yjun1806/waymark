description = "Create a new Waymark issue doc (Why/How/Tasks/Decisions) in waymark/draft/"

prompt = """
Create a new Waymark issue document by following the **waymark-work** skill's flow: read
`.waymark.yml`; gather the title, target repos, planning source, and tracker issue links
(multiple OK); allocate exactly one id; and scaffold `waymark/draft/<id>-<slug>.md` from the
template. Invoke the waymark-work skill — if it isn't available, follow its steps directly.
"""
